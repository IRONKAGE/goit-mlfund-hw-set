# ==========================================
# 1. Core Frameworks & UI
# ==========================================
setuptools==81.0.0 # Надає модуль pkg_resources для зворотної сумісності (вимагається профайлером)
ruff==0.15.18 # Надшвидкий лінтер та форматер коду (Rust), використовується Marimo
marimo==0.23.11 # Реактивне Python-середовище нового покоління (заміна Jupyter)
streamlit==1.58.0 # Побудова інтерактивних BI-дашбордів для презентації моделей
plotly==6.8.0 # Багатовимірна інтерактивна візуалізація та аналіз залишків
networkx==3.6.1 # Математичний бекенд для розрахунку топології графів (GraphRAG)
ipysigma==0.24.6 # WebGL-фронтенд для рендерингу графів (заміна застарілому pyvis)
python-dotenv==1.2.2 # Безпечне керування секретами та конфігураціями (API ключі)
psutil==7.2.2 # Системний моніторинг використання RAM та CPU під час навчання
kaggle==2.2.2 # CLI-клієнт для автоматизованого завантаження наборів даних

# ==========================================
# 2. Data Processing & EDA (Memory Optimized)
# ==========================================
pandas==2.3.3 # Стабільна LTS-гілка з підтримкою PyArrow та Copy-on-Write (оптимізація RAM)
pyarrow==24.0.0 # Високопродуктивний бекенд пам'яті для Pandas (на базі C++ Apache Arrow)
polars==1.42.1 # Надшвидкий рушій (Rust). Нативний стандарт для UI-таблиць Marimo (гарантує імутабельність та усуває помилки 'read-only' при сортуванні)
numpy==1.26.4 # LTS-версія для збереження ABI-сумісності C++ компіляторів на старих архітектурах macOS (Intel/AMD)
scipy==1.16.3 # "Мостова" версія: сумісна зі старим NumPy 1.x та задовольняє суворі вимоги профайлера
tqdm==4.68.3 # Моніторинг прогресу виконання важких циклів та завантажень (CLI/GUI)
fg-data-profiling==4.19.1 # Автоматичний розвідувальний аналіз (EDA) та Data Drift (колишній ydata-profiling)
gdown==6.1.0 # Утиліта для прямого завантаження великих наборів даних з Google Drive

# ==========================================
# 3. Machine Learning Algorithms
# ==========================================
scikit-learn==1.9.0 # Класичне ML ядро (використовується нативний Pandas API)
xgboost==3.3.0 # Екстремальний градієнтний бустинг із нативною підтримкою категорій
lightgbm==4.6.0 # Високопродуктивний бустинг від Microsoft для великих таблиць
interpret==0.7.8 # Explainable Boosting Machine (EBM): інтерпретований ML від Microsoft (EBM) для глибокого розуміння впливу кожної фічі
imbalanced-learn==0.14.2 # Боротьба з дисбалансом класів (SMOTE, ADASYN)
prophet==1.3.0 # SOTA-алгоритм для прогнозування часових рядів від Meta

# ==========================================
# 4. MLOps: Tracking & Serialization
# ==========================================
mlflow==3.14.0 # Трекінг експериментів, логування гіперпараметрів та збереження артефактів
joblib==1.5.3 # Оптимізована серіалізація ML-пайплайнів (набагато швидше за pickle)
optuna==4.9.0 # Просунутий фреймворк для Байєсівської оптимізації гіперпараметрів моделей (Smart Tuning)

# ==========================================
# 5. Foundation Models & Acceleration
# ==========================================
# ⚠️ АРХІТЕКТУРНЕ РІШЕННЯ: Динамічна маршрутизація версій PyTorch
# Для Apple Silicon, Windows та Linux встановлюється найновіша версія (2.12.1)
torch==2.12.1 ; sys_platform != 'darwin' or platform_machine != 'x86_64'
# Для Legacy-систем macOS на базі Intel (x86_64) встановлюється стабільна 2.2.2
torch==2.2.2 ; sys_platform == 'darwin' and platform_machine == 'x86_64'
# Ці моделі є універсальними і сумісними з обома версіями PyTorch (>=2.0.0)
sentence-transformers==5.6.0 # Створення щільних векторних представлень (ембедингів) для семантичного пошуку та NLP
timesfm==2.0.1 # Foundation Model від Google для високого zero-shot прогнозування часових рядів (Time Series)

# ==========================================
# 6. Model Serving & API (Production)
# ==========================================
fastapi==0.138.0 # Сучасний асинхронний фреймворк для створення ML API
uvicorn==0.49.0 # ASGI-сервер для запуску FastAPI на високих швидкостях
pydantic==2.13.4 # Сувора валідація вхідних даних (JSON) для моделей
